CREATE DATABASE tyrell_corp;
USE tyrell_corp;
CREATE TABLE nexus6 (user varchar(10), password varchar(10));
INSERT INTO nexus6 VALUES ('user', 'password');
GRANT ALL PRIVILEGES ON tyrell_corp.* TO 'holberton_user'@'localhost';
FLUSH PRIVILEGES;
